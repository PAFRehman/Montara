[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python pythos_bot.py"
healthcheckPath = "/"
healthcheckPort = 8080
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
